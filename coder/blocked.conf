# Returns 403 Forbidden for blocked requests
server {
    listen 80;
    server_name _;

    location / {
        default_type application/json;
        return 403 '{"message": "Forbidden", "detail": "This action requires admin authorization. Include X-Admin-Secret header."}';
    }
}
